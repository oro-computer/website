# `std::io::stream`

Source: `std/io/stream.slk`

This is the exact canonical documentation page for `std::io::stream`.

## Role

`std::io::stream` is a shipped nested module in the Silk standard library.
This exact-name page exists so the module can be discovered and referenced directly by its canonical name.

## Canonical Context

Use the following owning docs for the substantive API/design context for this module:

- [io](?p=std/io)
- [stream](?p=std/stream)

## Notes

- The shipped source for this module is `std/io/stream.slk`.
- The canonical module name is `std::io::stream`.
- Family-wide semantics, examples, and cross-module relationships live in the owning docs listed above.

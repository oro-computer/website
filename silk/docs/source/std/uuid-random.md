# `std::uuid::random`

Source: `std/uuid/random.slk`

This is the exact canonical documentation page for `std::uuid::random`.

## Role

`std::uuid::random` is a shipped nested module in the Silk standard library.
This exact-name page exists so the module can be discovered and referenced directly by its canonical name.

## Canonical Context

Use the following owning docs for the substantive API/design context for this module:

- [uuid](?p=std/uuid)

## Notes

- The shipped source for this module is `std/uuid/random.slk`.
- The canonical module name is `std::uuid::random`.
- Family-wide semantics, examples, and cross-module relationships live in the owning docs listed above.

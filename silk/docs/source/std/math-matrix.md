# `std::math::matrix`

Source: `std/math/matrix.slk`

This is the exact canonical documentation page for `std::math::matrix`.

## Role

`std::math::matrix` is a shipped nested module in the Silk standard library.
This exact-name page exists so the module can be discovered and referenced directly by its canonical name.

## Canonical Context

Use the following owning docs for the substantive API/design context for this module:

- [math](?p=std/math)

## Notes

- The shipped source for this module is `std/math/matrix.slk`.
- The canonical module name is `std::math::matrix`.
- Family-wide semantics, examples, and cross-module relationships live in the owning docs listed above.

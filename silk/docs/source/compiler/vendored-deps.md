# Built-In Dependencies (Legacy Link)

This page moved to [builtin deps](?p=compiler/builtin-deps). The canonical built-in dependency documentation describes the shipped dependency sources, generated archives, provider selection, staging layout, and `zig build deps` workflow.
